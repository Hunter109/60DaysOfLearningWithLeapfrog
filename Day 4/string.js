let college = "United Technical College";
let university = "Pokhara University"

// we can check the length of the csring 
console.log(`the length of the string is ${college.length}`)
console.log(`the length of the string is ${university.length}`)